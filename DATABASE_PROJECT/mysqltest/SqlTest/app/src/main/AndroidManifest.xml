<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manuel.testsql" >

    <uses-sdk
        android:minSdkVersion="15"
        android:maxSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".users.ChangePassword"/>
        <activity android:name=".users.LoggedIn"/>
        <activity android:name=".users.Login" />
        <activity android:name=".users.PasswordReset"/>
        <activity android:name=".users.Register" />
        <activity android:name=".users.Registered"/>
    </application>

</manifest>
